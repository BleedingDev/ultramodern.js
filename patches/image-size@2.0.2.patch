diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608..04fa314 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -158,20 +172,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -188,6 +207,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -250,10 +270,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize2(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -288,8 +315,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -368,6 +395,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -470,7 +500,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -485,10 +515,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab..202810e 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -156,20 +170,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -186,6 +205,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -248,10 +268,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize2(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -286,8 +313,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -366,6 +393,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -468,7 +498,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -483,10 +513,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3d..10bd3a6 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -53,22 +53,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -184,20 +198,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -214,6 +233,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -276,10 +296,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -314,8 +341,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -394,6 +421,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -496,7 +526,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -511,10 +541,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c444..498c9ad 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -30,22 +30,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -161,20 +175,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -191,6 +210,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -253,10 +273,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -291,8 +318,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -371,6 +398,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -473,7 +503,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -488,10 +518,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210..e0f3bfc 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -29,22 +29,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -160,20 +174,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -190,6 +209,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -252,10 +272,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -290,8 +317,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -370,6 +397,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -472,7 +502,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -487,10 +517,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17af..c90f6a4 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -156,20 +170,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -186,6 +205,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -248,10 +268,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -286,8 +313,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -366,6 +393,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -468,7 +498,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -483,10 +513,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8..806ad4b 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -158,20 +172,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -188,6 +207,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -250,10 +270,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -288,8 +315,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -368,6 +395,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -470,7 +500,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -485,10 +515,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c..1e088a7 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -156,20 +170,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -186,6 +205,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -248,10 +268,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
-      images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      if (imageSize2.width && imageSize2.height) {
+        images.push(imageSize2);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -286,8 +313,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -366,6 +393,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -468,7 +498,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -483,10 +513,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f49078..4fc2d1e 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -5,22 +5,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -56,20 +70,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 0133091..7d0e835 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -3,22 +3,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -54,20 +68,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d37..e69d509 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -9,6 +9,7 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 // lib/types/icns.ts
 var SIZE_HEADER = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -71,10 +72,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027..4e14ac4 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -7,6 +7,7 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 // lib/types/icns.ts
 var SIZE_HEADER = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -69,10 +70,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd1..30894ae 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -158,20 +172,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -188,6 +207,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -250,10 +270,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize2(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -288,8 +315,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -368,6 +395,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -470,7 +500,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -485,10 +515,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda..a00639b 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -156,20 +170,25 @@ var HEIF = {
     const type = toUTF8String(input, 8, 12);
     const images = [];
     let currentOffset = ipcoBox.offset + 8;
-    while (currentOffset < ipcoBox.offset + ipcoBox.size) {
+    const ipcoEnd = ipcoBox.offset + ipcoBox.size;
+    while (currentOffset < ipcoEnd) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size < 8) break;
+      if (ispeBox.offset + ispeBox.size > ipcoEnd) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
       let width = rawWidth;
       let height = rawHeight;
-      if (clapBox && clapBox.offset < ipcoBox.offset + ipcoBox.size) {
+      if (clapBox && clapBox.offset < ipcoEnd && clapBox.size >= 8) {
         const cropRight = readUInt32BE(input, clapBox.offset + 12);
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      const nextOffset = ispeBox.offset + ispeBox.size;
+      if (nextOffset <= currentOffset) break;
+      currentOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -186,6 +205,7 @@ var HEIF = {
 // lib/types/icns.ts
 var SIZE_HEADER2 = 4 + 4;
 var FILE_LENGTH_OFFSET = 4;
+var MIN_ENTRY_LENGTH = 8;
 var ENTRY_LENGTH_OFFSET = 4;
 var ICON_TYPE_SIZE = {
   ICON: 32,
@@ -248,10 +268,17 @@ var ICNS = {
     let imageOffset = SIZE_HEADER2;
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
+      if (inputLength - imageOffset < MIN_ENTRY_LENGTH) break;
       const imageHeader = readImageHeader(input, imageOffset);
+      const entryLength = imageHeader[1];
+      if (entryLength < MIN_ENTRY_LENGTH) break;
       const imageSize = getImageSize2(imageHeader[0]);
-      images.push(imageSize);
-      imageOffset += imageHeader[1];
+      if (imageSize.width && imageSize.height) {
+        images.push(imageSize);
+      }
+      const nextOffset = imageOffset + entryLength;
+      if (nextOffset <= imageOffset) break;
+      imageOffset = nextOffset;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -286,8 +313,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
@@ -366,6 +393,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
@@ -468,7 +498,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -483,10 +513,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/jp2.cjs b/dist/types/jp2.cjs
index cdd7c88..c544de6 100644
--- a/dist/types/jp2.cjs
+++ b/dist/types/jp2.cjs
@@ -5,22 +5,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -36,8 +50,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
diff --git a/dist/types/jp2.mjs b/dist/types/jp2.mjs
index 63a5376..9dba8be 100644
--- a/dist/types/jp2.mjs
+++ b/dist/types/jp2.mjs
@@ -3,22 +3,36 @@ var decoder = new TextDecoder();
 var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(input.slice(start, end));
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -34,8 +48,8 @@ var JP2 = {
   },
   calculate(input) {
     const jp2hBox = findBox(input, "jp2h", 0);
-    const ihdrBox = jp2hBox && findBox(input, "ihdr", jp2hBox.offset + 8);
-    if (ihdrBox) {
+    const ihdrBox = jp2hBox && jp2hBox.size >= 8 && findBox(input, "ihdr", jp2hBox.offset + 8);
+    if (ihdrBox && ihdrBox.size >= 8) {
       return {
         height: readUInt32BE(input, ihdrBox.offset + 8),
         width: readUInt32BE(input, ihdrBox.offset + 12)
diff --git a/dist/types/jpg.cjs b/dist/types/jpg.cjs
index 664da4a..78b3c1e 100644
--- a/dist/types/jpg.cjs
+++ b/dist/types/jpg.cjs
@@ -89,6 +89,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
diff --git a/dist/types/jpg.mjs b/dist/types/jpg.mjs
index 8fd6076..95fdb6b 100644
--- a/dist/types/jpg.mjs
+++ b/dist/types/jpg.mjs
@@ -87,6 +87,9 @@ var JPG = {
     let next;
     while (input.length) {
       const i = readUInt16BE(input, 0);
+      if (i < 2) {
+        throw new TypeError("Corrupt JPG, invalid segment length");
+      }
       validateInput(input, i);
       if (input[i] !== 255) {
         input = input.slice(1);
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095b..89d6550 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -46,22 +46,36 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var toHexString = (input, start = 0, end = input.length) => input.slice(start, end).reduce((memo, i) => memo + `0${i.toString(16)}`.slice(-2), "");
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -101,7 +115,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -116,10 +130,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4c..50a5e9b 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -44,22 +44,36 @@ var toUTF8String = (input, start = 0, end = input.length) => decoder.decode(inpu
 var toHexString = (input, start = 0, end = input.length) => input.slice(start, end).reduce((memo, i) => memo + `0${i.toString(16)}`.slice(-2), "");
 var getView = (input, offset) => new DataView(input.buffer, input.byteOffset + offset);
 var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, false);
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
@@ -99,7 +113,7 @@ var JXLStream = {
 // lib/types/jxl.ts
 function extractCodestream(input) {
   const jxlcBox = findBox(input, "jxlc", 0);
-  if (jxlcBox) {
+  if (jxlcBox && jxlcBox.size >= 8) {
     return input.slice(jxlcBox.offset + 8, jxlcBox.offset + jxlcBox.size);
   }
   const partialStreams = extractPartialStreams(input);
@@ -114,10 +128,13 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size < 12) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    const nextOffset = jxlpBox.offset + jxlpBox.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
   return partialStreams;
 }
diff --git a/dist/types/utils.cjs b/dist/types/utils.cjs
index 23d52e4..03a252b 100644
--- a/dist/types/utils.cjs
+++ b/dist/types/utils.cjs
@@ -27,22 +27,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
diff --git a/dist/types/utils.mjs b/dist/types/utils.mjs
index 3f09dce..95d66f7 100644
--- a/dist/types/utils.mjs
+++ b/dist/types/utils.mjs
@@ -25,22 +25,36 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
   const methodName = `readUInt${bits}${endian}`;
   return methods[methodName](input, offset);
 }
+var MIN_BOX_HEADER = 8;
 function readBox(input, offset) {
-  if (input.length - offset < 4) return;
+  if (input.length - offset < MIN_BOX_HEADER) return;
   const boxSize = readUInt32BE(input, offset);
+  if (boxSize === 0) {
+    return {
+      name: toUTF8String(input, offset + 4, offset + 8),
+      offset,
+      size: input.length - offset
+    };
+  }
+  if (boxSize === 1) return;
+  if (boxSize < MIN_BOX_HEADER) return;
   if (input.length - offset < boxSize) return;
   return {
-    name: toUTF8String(input, 4 + offset, 8 + offset),
+    name: toUTF8String(input, offset + 4, offset + 8),
     offset,
     size: boxSize
   };
 }
-function findBox(input, boxName, currentOffset) {
-  while (currentOffset < input.length) {
-    const box = readBox(input, currentOffset);
+function findBox(input, boxName, startOffset) {
+  let offset = startOffset;
+  while (offset < input.length) {
+    const box = readBox(input, offset);
     if (!box) break;
     if (box.name === boxName) return box;
-    currentOffset += box.size > 0 ? box.size : 8;
+    if (box.size < MIN_BOX_HEADER) break;
+    const nextOffset = offset + box.size;
+    if (nextOffset <= offset) break;
+    offset = nextOffset;
   }
 }
 
