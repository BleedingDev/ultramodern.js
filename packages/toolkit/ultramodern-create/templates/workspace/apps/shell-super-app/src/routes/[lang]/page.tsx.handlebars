import { Link, useModernI18n } from '@modern-js/plugin-i18n/runtime';
import ShellFrame from '../shell-frame';
import { UltramodernRouteHead } from '../ultramodern-route-head';
import { VerticalShowcase } from '../vertical-components';
import { ultramodernUiMarker } from '../../ultramodern-build';

export default function ShellHome() {
  const { t } = useModernI18n();

  return (
    <ShellFrame>
      <UltramodernRouteHead />
      <section className="{{heroClassName}}">
        <div className="{{heroContentClassName}}">
          <p className="{{eyebrowClassName}}">{t('shell.hero.eyebrow')}</p>
          <h1 className="{{titleClassName}}">{t('shell.title')}</h1>
          <p className="{{ledeClassName}}">{t('shell.hero.lede')}</p>
          <div className="{{actionsClassName}}">
            <Link className="{{primaryActionClassName}}" to="/">
              {t('shell.hero.primary')}
            </Link>
            <span className="{{secondaryActionClassName}}">
              {t('shell.hero.secondary')}
            </span>
          </div>
        </div>
        <div className="{{cardsClassName}}">
          <div className="{{cardsGridClassName}}">
            <article className="{{remoteCardClassName}}">
              <span className="{{remoteCardKickerClassName}}">{t('shell.hero.cardOneKicker')}</span>
              <strong className="{{cardValueClassName}}">{{remoteCount}}</strong>
              <p className="{{cardBodyClassName}}">{t('shell.hero.cardOne')}</p>
            </article>
            <article className="{{ssrCardClassName}}">
              <span className="{{ssrCardKickerClassName}}">{t('shell.hero.cardTwoKicker')}</span>
              <strong className="{{cardValueClassName}}">SSR</strong>
              <p className="{{cardBodyClassName}}">{t('shell.hero.cardTwo')}</p>
            </article>
          </div>
        </div>
      </section>
      <VerticalShowcase />
      <p className="{{hiddenClassName}}" data-testid="ultramodern-preset">presetUltramodern workspace</p>
      <p className="{{hiddenClassName}}" data-build-marker={ultramodernUiMarker.build} data-testid="ultramodern-ui-marker">
        {ultramodernUiMarker.appId}:{ultramodernUiMarker.version}
      </p>
    </ShellFrame>
  );
}
