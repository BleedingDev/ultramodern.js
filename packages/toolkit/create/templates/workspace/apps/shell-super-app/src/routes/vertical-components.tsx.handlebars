{{value0}}import { Link, useModernI18n } from '@modern-js/plugin-i18n/runtime';

	const widgetCount = Number('{{value1}}');

	{{value2}}
	{{value3}}

	export const Header = () => {
  const { t } = useModernI18n();

  return (
    <header className="{{value4}}" data-modern-boundary-id="{{value5}}" data-modern-mf-expose="shell/Header">
      <Link className="{{value6}}" to="/">{t('shell.title')}</Link>
    </header>
  );
};

export const StatusBadge = () => {
  const { t } = useModernI18n();

  return (
    <span className="{{value7}}">
      {widgetCount} {t('shell.hero.cardOneKicker')}
    </span>
  );
};

export const VerticalShowcase = () => {
  const { t } = useModernI18n();

  if (widgetCount === 0) {
    return (
      <section className="{{value8}}">
        <p className="{{value9}}">{t('shell.hero.empty')}</p>
      </section>
    );
  }

  return (
    <section className="{{value10}}" data-modern-boundary-id="{{value11}}">
      {{value12}}
    </section>
  );
};
