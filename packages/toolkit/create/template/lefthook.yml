pre-commit:
  commands:
    fix-and-check:
      run: pnpm format && pnpm lint:fix && pnpm ultramodern:check
      stage_fixed: true

pre-push:
  commands:
    check:
      run: pnpm ultramodern:check
