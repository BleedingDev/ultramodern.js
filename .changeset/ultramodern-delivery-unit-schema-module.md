---
'@modern-js/create': patch
---

Add the canonical delivery-unit schema module (`DeliveryUnitDescriptor`, `SurfaceDescriptor`, `SurfaceRef` parser/formatter, `ResolvedDeliveryUnit`, v1 down-projection with unknown-field preservation) as internal, unwired code with its spec, plus public-surface and strict-dispatch characterization tests. No public exports or behavior change.
