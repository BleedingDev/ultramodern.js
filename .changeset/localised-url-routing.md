---
'@modern-js/plugin-i18n': minor
---

Add opt-in `localisedUrls` routing support for locale-specific URL pathnames.
The feature activates only when a non-empty localisation map is configured;
upstream-shaped configs without a map are untouched.
