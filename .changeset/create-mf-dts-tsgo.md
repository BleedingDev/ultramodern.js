---
'@modern-js/ultramodern-create': minor
'@modern-js/sandpack-react': patch
---

feat(create): generate Module Federation DTS with `tsgo` (`dts.generateTypes.compilerInstance: 'tsgo'`) instead of fetching `typescript` through npx, using the workspace `@typescript/native-preview` install
