---
'@modern-js/ultramodern-create': patch
---

Add MicroVertical dry-run planning through `planUltramodernVertical` and the `create <name> --vertical --dry-run` CLI flag, returning created paths, rewrites, JSON mutations, port and Module Federation metadata, Effect API prefixes, dependency changes, and generated contract changes without writing to the workspace.
