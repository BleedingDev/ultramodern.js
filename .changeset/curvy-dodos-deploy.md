---
'@modern-js/app-tools': patch
---

Preserve npm package aliases in Node deployment output so renamed framework distributions remain directly runnable and reinstallable. Evolve the final MicroVertical release envelope to schema v3 so regular-file bytes and in-root symbolic-link topology are independently and strictly bound.
