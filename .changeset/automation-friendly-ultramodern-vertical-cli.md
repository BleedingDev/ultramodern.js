---
'@modern-js/ultramodern-create': patch
---

Add automation-friendly MicroVertical CLI syntax with `--vertical=<name>` and `--vertical-name <name>`, while preserving `<name> --vertical` and rejecting missing or ambiguous vertical names before writing files.
