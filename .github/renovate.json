{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "dependencyDashboard": true,
  "minimumReleaseAge": "1 day",
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "rangeStrategy": "bump",
  "schedule": ["before 5am on monday"],
  "timezone": "Etc/UTC",
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "groupName": "github-actions",
      "labels": ["dependencies", "github-actions", "security"]
    },
    {
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["patch", "minor"],
      "groupName": "npm minor and patch updates",
      "labels": ["dependencies", "npm"]
    },
    {
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "major"]
    },
    {
      "description": "Fork pin lattice: @module-federation/* is patched via pnpm patchedDependencies at an exact version (pnpm-workspace.yaml); any bump must move the patches in lockstep or install hard-fails. Keep out of the weekly grouped PR.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["@module-federation/**"],
      "groupName": "module-federation (patched pin lattice)",
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "pin-lattice"]
    },
    {
      "description": "Fork pin lattice: TanStack Router versions are exact-pinned in plugin-runtime/plugin-tanstack and mirrored by packages/toolkit/ultramodern-create/src/ultramodern-workspace/versions.ts plus the release gates; bumps must update all of them in lockstep.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["@tanstack/**"],
      "groupName": "tanstack (gate-pinned lattice)",
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "pin-lattice"]
    },
    {
      "description": "Fork pin lattice: react-router/react-router-dom are pinned to an exact version via pnpm-workspace.yaml overrides; a package.json bump silently no-ops until the override moves with it.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["react-router", "react-router-dom"],
      "groupName": "react-router (override-pinned)",
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "pin-lattice"]
    },
    {
      "description": "Fork i18n surface: i18next is exact-pinned in packages/runtime/plugin-i18n devDependencies and react-i18next must satisfy the plugin's peer floor and MF/i18n integration fixtures; bump them together, outside the grouped weekly PR.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["react-i18next", "i18next"],
      "groupName": "i18next (fork i18n surface)",
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "pin-lattice"]
    },
    {
      "description": "Fork BFF contract surface: farrow-api/pipeline/schema back the fork-owned @modern-js/bff-runtime adapters; keep bumps out of the grouped weekly PR so a farrow regression cannot poison it.",
      "matchManagers": ["npm"],
      "matchPackageNames": ["farrow-api", "farrow-pipeline", "farrow-schema"],
      "groupName": "farrow (bff contract surface)",
      "dependencyDashboardApproval": true,
      "labels": ["dependencies", "pin-lattice"]
    }
  ]
}
