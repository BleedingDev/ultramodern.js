---
name: generator-source-cuts
overview: "Consolidate remaining repeated scaffold markup and positional template inputs without changing the generated UI."
todos:
  - id: generator-source-cuts-templates
    content: "Combine repeated remote scaffold source bodies through named inputs and replace anonymous shell valueN fields without adding a template framework."
    status: completed
  - id: generator-source-cuts-prove
    content: "Verify 21 generated-source parity cases and 13 retained generator behavior checks for deterministic formatting, i18n boundaries, native links and expose/SSR markers; remove only superseded render bodies and positional glue."
    status: completed
isProject: false
---

# generator-source-cuts

## Execution Notes

Own ultramodern-create/src/ultramodern-workspace/demo-components.ts and related shell page template inputs. The handwritten JSX scanner and framework migration command have already been deleted. Do not recreate codemods, compatibility recognizers or retired command entry points.

Audit: [current fork evidence](../../../docs/audits/fork-simplification-20260922.md). Final audited fork `ba2f373ad9587642062efc65c763276a68aee909`; fixed vanilla ownership base `eded841256a7cffdaa622e3889fc83407debd3e4`. Beads `modernjs-wuutt.4` under `modernjs-wuutt` is authoritative; keep this execution projection aligned. Source consolidation and generator-level verification are complete. Downstream Tractor acceptance is owned solely by plan90 (`integrated-feature-parity-tractor`).

## Constraints

Preserve current features, consumer ownership, database migrations and platform interoperability. Latest AGENTS.md requires direct removal of obsolete APIs, aliases and framework migration compatibility because there is no external adoption requirement. Do not restore already-retired code. New subsystems stay fork-owned; native non-shrink edits need same-PR ledger evidence. No app workaround, source import across vertical ownership, hand-edited generated output/lockfile, weakened gate or arbitrary deletion quota. Reuse existing owners; relocation is not a cut. New test deletion proposals require explicit selection, and approved prior deletions must not be silently reversed.

## Operator Guidance

Preserve locale-relative paths, component/expose identity, translation keys, Tailwind prefixes, accessibility, native TanStack links and SSR fragment markers. Retained tests: workspace-determinism, generated-workspace-format, generated-workspace-lint and vertical-presets-protocol-horizontal. Three current duplicated functions span about 94 lines before replacement; only their duplicated portion is a candidate. Final downstream Tractor visual parity is required under plan90 (`integrated-feature-parity-tractor`); it is not a prerequisite for completing this generator lane.

Depends on preservation-baseline and blocks integrated-feature-parity. Siblings may run in parallel within the named ownership boundaries. Native Codex workers are not alone in the codebase and must preserve other owners edits. Shared file changes go through the root integrator.

Use the exact selection/edges in docs/audits/fork-simplification-20260922-handoff.md. Validate before plan-backed launches. Run relevant current behavior checks, report limitations and include concrete removed decision owners plus net authored-code change in the PR.

## Lane verification

One remote surface renderer now owns entry, widget and custom expose markup, including its i18n setup and boundary. Shell page, header/showcase and unavailable-state templates use named inputs; identical card classes and boundary IDs share inputs. Removed two repeated remote render bodies, two single-use string wrappers and unused shell-id template input. Net authored source reduction: 56 lines; template line count is unchanged.

Twenty-one generated outputs were compared byte-for-byte with a pre-edit capture: three remote descriptors (including domain fallback), route entry, Widget/custom/Route exposes, and shell page plus Node/worker composition with zero/one/three remotes. All matched, preserving native links, locale paths, translation keys, Tailwind classes and expose markers. Biome passes. Retained generator determinism, formatting, lint and preset/protocol/horizontal suites pass (4 files, 13 tests). No test files or manifests changed. The generator proof item is complete on this evidence. Tractor acceptance has not run in this lane; plan90 alone verifies the final combined graph output downstream.
