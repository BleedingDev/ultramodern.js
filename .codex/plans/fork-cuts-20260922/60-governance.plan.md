---
name: governance-representation
overview: "Share immutable ownership projection and make current ledger evidence structured while preserving historical proof and separate AST policy constraints."
todos:
  - id: governance-representation-ownership
    content: "Derive resolved ownership/import decisions from fixed-base and rename facts; prove old/new gate equivalence with retained checks and compact direct adversarial cases for currently unprotected classifier behavior."
    status: completed
  - id: governance-representation-ledger
    content: "Introduce strict versioned structured evidence inside the same FORK-DIVERGENCE.md and generate its readable tables with a reviewed lossless migration."
    status: completed
  - id: governance-representation-retire
    content: "Retire duplicated native inventory and current Markdown input after fail-closed parity; retain historical-only grammar and distinct AST policy safeguards."
    status: completed
isProject: false
---

# governance-representation

## Execution Notes

Own scripts/ultramodern-boundary-check and the ledger representation migration. Treat FORK-DIVERGENCE.md as a root integration file: coordinate lane ledger entries and integrate them without overwriting concurrent evidence. Do not change fixed base, canonical scope, import-reference semantics or allowed budgets to make results pass. New schema acceptance needs documented migration and parity evidence.

Audit: [current fork evidence](../../../docs/audits/fork-simplification-20260922.md). Final audited fork `ba2f373ad9587642062efc65c763276a68aee909`; fixed vanilla ownership base `eded841256a7cffdaa622e3889fc83407debd3e4`. Beads `modernjs-wuutt.10` under `modernjs-wuutt` is authoritative; keep this execution projection aligned. Implementation completed with the safety qualifications and evidence below.

## Constraints

Preserve current features, consumer ownership, database migrations and platform interoperability. Latest AGENTS.md requires direct removal of obsolete APIs, aliases and framework migration compatibility because there is no external adoption requirement. Do not restore already-retired code. New subsystems stay fork-owned; native non-shrink edits need same-PR ledger evidence. No app workaround, source import across vertical ownership, hand-edited generated output/lockfile, weakened gate or arbitrary deletion quota. Reuse existing owners; relocation is not a cut. New test deletion proposals require explicit selection, and approved prior deletions must not be silently reversed.

## Operator Guidance

Current gate is zero governed imports and 816 files / 3,076 hunks / 50,882 lines. Preserve unknown ownership, unresolved refs, symlinks/aliases, policy smuggling, rename/provenance, canonical scope, same-PR evidence, componentwise shrink and equal-count replacement. divergence.test.js and old classifier mutations were deleted; do not call them retained or restore the full corpus. Use current checker tests and focused malformed/renamed/equal-count evidence cases for this changed enforcement. No package-wide exemption, alternate ledger or budget reset.

Depends on preservation-baseline and blocks integrated-feature-parity. Siblings may run in parallel within the named ownership boundaries. Native Codex workers are not alone in the codebase and must preserve other owners edits. Shared file changes go through the root integrator.

Use the exact selection/edges in docs/audits/fork-simplification-20260922-handoff.md. Validate before plan-backed launches. Run relevant current behavior checks, report limitations and include concrete removed decision owners plus net authored-code change in the PR.

## Implementation evidence and qualified scope

The import scanner now uses the same immutable rename projection as divergence.
Native create-request dependency/source identity comes from its fixed audited
package instead of independently maintained inventories; the two reviewed neutral
extractions remain explicit. Repeated manifest key checks have one owner.

Current governance accepts only the versioned `fork-evidence:v1` JSON block in
FORK-DIVERGENCE.md. Its readable table is generated and checked by the default
gate. All 472 previously valid semantic evidence keys migrated unchanged; five
invalid advisory rows remain non-authorizing. A format migration alone produces
zero new same-PR evidence. New schema, disposition, duplicate and malformed data
checks fail closed. The canonical bases, scope and budgets are unchanged.

The parent integrator accepted two necessary qualifications: historical Git
merge bases still require the legacy read-only grammar to prevent laundering old
rows, and package ownership cannot replace AST policy checks for inline retired
policy identifiers, unexpected public surfaces, aliases or symlinks. Those
safeguards remain. No claim is made that the entire 400-line classifier or every
historical parser was safely deletable. This lane improves authority and removes
inventory duplication; it is a net code addition, not a LOC saving.

Validation: 10 focused behavior tests pass, including historical-to-structured
Git evidence, equal-count replacement, rename ownership, native request policy,
alias, unknown-file and symlink rejection. Nine built-in self-tests, Biome and
the generated table check pass. Full canonical scan ran: zero governed imports
across 602 native identities; cumulative measurement detected concurrent i18n
lane changes requiring root integration, with no budget edits by this lane.

Independent review correction: structured owner/reason text now uses the exact
historical semantic normalizer. Formatting, emphasis and invisible comments
cannot create new same-PR evidence; formatting-only required fields are rejected.
The Git regression proves this against both historical and structured merge
bases. Independent recheck passed all 10 tests and table validation, confirmed
all original 472 semantic keys unchanged, and confirmed root's five intentional
implementation entries are the only additions. No remaining governance review
findings. Root also recorded the equal-count native request-handler test change.
