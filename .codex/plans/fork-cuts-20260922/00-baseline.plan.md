---
name: preservation-baseline
overview: "Freeze required behavior, audited ownership and existing-work reconciliation before any deletion."
todos:
  - id: preservation-baseline-reconcile
    content: "Reconcile completed native Effect inference, migration/legacy-registry retirement and approved test purge against ba2f373ad9; freeze the CURRENT supported feature and ownership matrix."
    status: completed
  - id: preservation-baseline-map-proofs
    content: "Map every ranked cut to retained behavior evidence, current public consumers and an owning package; record baseline production, template, test and governance measurements separately."
    status: completed
  - id: preservation-baseline-qualify
    content: "Run the existing targeted preservation checks, resolve unexpected failures and freeze shared contract decisions and file ownership for parallel implementation."
    status: completed
isProject: false
---

# preservation-baseline

## Execution Notes

Own the feature/ownership acceptance matrix and shared-file integration decisions. Read current AGENTS.md, CONTEXT.md, ADR-0019 and FORK-DIVERGENCE.md at ba2f373ad9. No external adoption compatibility is required; do not restore retired migration APIs, client generation or mixed-version registries. Map current tests after the purge. Assign exact worker-entry filenames/export metadata and default-policy integration ownership to the build lane before parallel launch.

Audit: [current fork evidence](../../../docs/audits/fork-simplification-20260922.md). Final audited fork `ba2f373ad9587642062efc65c763276a68aee909`; fixed vanilla ownership base `eded841256a7cffdaa622e3889fc83407debd3e4`. Beads `modernjs-wuutt.1` under `modernjs-wuutt` is authoritative; keep this execution projection aligned. All implementation is pending.

## Constraints

Preserve current features, consumer ownership, database migrations and platform interoperability. Latest AGENTS.md requires direct removal of obsolete APIs, aliases and framework migration compatibility because there is no external adoption requirement. Do not restore already-retired code. New subsystems stay fork-owned; native non-shrink edits need same-PR ledger evidence. No app workaround, source import across vertical ownership, hand-edited generated output/lockfile, weakened gate or arbitrary deletion quota. Reuse existing owners; relocation is not a cut. New test deletion proposals require explicit selection, and approved prior deletions must not be silently reversed.

## Operator Guidance

Record each required current behavior, its remaining proof or concrete gap, replacement owner and final acceptance owner. Resolve the production remote-address disagreement explicitly. Freeze public MF interoperability, strict delivery identity, runtime platforms and consumer ownership. Keep a small set of direct behavior/invariant checks, not the deleted test corpus. Reconcile Beads state against actual source; older local issues can be stale.

Sole graph root. Finish current behavior, assurance-gap and exact shared-file ownership decisions before implementation lanes.

Use the exact selection/edges in docs/audits/fork-simplification-20260922-handoff.md. Validate before plan-backed launches. Run relevant current behavior checks, report limitations and include concrete removed decision owners plus net authored-code change in the PR.
